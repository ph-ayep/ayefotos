export default function Partidos() {
  const partidos = [
    {
      id: 1,
      equipos: "Equipo A vs Equipo B",
      fecha: "28/07/2026",
      fotos: 250,
    },
    {
      id: 2,
      equipos: "Equipo C vs Equipo D",
      fecha: "30/07/2026",
      fotos: 180,
    },
  ];

  return (
    <main style={{ padding: "40px" }}>
      <h1>AyeFotos 📸</h1>

      <h2>Partidos disponibles</h2>

      {partidos.map((partido) => (
        <div
          key={partido.id}
          style={{
            border: "1px solid #ddd",
            padding: "20px",
            marginTop: "20px",
            borderRadius: "10px",
          }}
        >
          <h3>{partido.equipos}</h3>

          <p>Fecha: {partido.fecha}</p>

          <p>Fotos disponibles: {partido.fotos}</p>

          <button>
            Ver fotos
          </button>
        </div>
      ))}
    </main>
  );
}
